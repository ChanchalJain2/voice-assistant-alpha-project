git clone "url"

cd directory

git checkout
(by default it will be main we can change by specifying the required after checkout)

git status

make new changes

git status

git add *

git status

git commit -m "Comments"

git push

